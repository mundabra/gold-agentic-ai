{{- if .Values.networkPolicy.enabled }}
{{- $root := . -}}
{{- $name := include "gold.fullname" . -}}
{{- /* Who may call each component. Egress is left open for model endpoints. The orchestrator
       calls mcp-crm itself to run actions a person approved, and writes feedback to Postgres. */}}
{{- $callers := dict
      "registry"          (list "orchestrator" "sql-agent" "definitions-agent" "account-agent")
      "sql-agent"         (list "orchestrator")
      "definitions-agent" (list "orchestrator")
      "account-agent"     (list "orchestrator")
      "mcp-data"          (list "sql-agent")
      "mcp-glossary"      (list "definitions-agent")
      "mcp-crm"           (list "account-agent" "orchestrator")
      "postgres"          (list "mcp-data" "mcp-glossary" "mcp-crm" "orchestrator")
      "scripted-model"    (list "orchestrator" "sql-agent" "definitions-agent" "account-agent")
      "gateway"           (list "orchestrator" "sql-agent" "definitions-agent" "account-agent" "guardrails")
      "guardrails"        (list "orchestrator") -}}
{{- range $target, $from := $callers }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $name }}-{{ $target }}
  labels: {{- include "gold.labels" $root | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: {{ $root.Release.Name }}
      app.kubernetes.io/component: {{ $target }}
  policyTypes: ["Ingress"]
  ingress:
    - from:
        {{- range $from }}
        - podSelector:
            matchLabels:
              app.kubernetes.io/instance: {{ $root.Release.Name }}
              app.kubernetes.io/component: {{ . }}
        {{- end }}
---
{{- end }}
{{- end }}
